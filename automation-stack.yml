services:
  n8n:
    image: docker.n8n.io/n8nio/n8n
    ports:
      - "5678:5678"
    volumes:
      - n8n_data:/home/node/.n8n
    networks:
      - proxy
    deploy:
      mode: replicated
      replicas: 1

volumes:
  n8n_data:

networks:
  proxy:
    external: true